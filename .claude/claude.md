# dd.yaml

`dd.yaml` is a data dictionary specification for describing collections of related tables: their contents, constraints, connections, and domain vocabulary. It's designed to be co-written by humans and AI agents.

The repo contains:

- `spec.md`: the full specification
- `examples/`: example data dictionaries downloaded from other repos (see `download-examples.R`)
- `README.md`: project overview and motivation

## Prose

- Use sentence case for headings.

## Code

- Use nanoparquet for reading/writing parquet files
